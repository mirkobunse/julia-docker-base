# registered packages are installed by simply adding them to the REQUIRE file, but
# un-registered ones need to be cloned in this script. Any other build step can
# also be triggered from here.
info("Setup complete")
